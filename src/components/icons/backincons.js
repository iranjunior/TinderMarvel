import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';
import {View} from 'react-native';

const back = () => (
  <View>
    <Icon name="ios-arrow-back" size={18} color={'#000'} />
  </View>
);

export default back;
