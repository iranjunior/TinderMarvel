import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';
import {View} from 'react-native';

const back = () => (
  <View>
    <Icon name="md-undo" size={32} color={'#fff'} />
  </View>
);

export default back;
